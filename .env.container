# Container Environment Configuration
# Copy to .env.container and adjust values for your setup
#
# Usage:
#   podman run --rm --env-file .env.container -p 8000:8000 pjs-neo-rag
#
# Note: When running in a container, use container/host networking:
#   - If using podman-compose: use service names (neo4j, ollama)
#   - If running standalone with --network host: use localhost
#   - If running standalone without host network: use host.containers.internal

# ==== Neo4j ====
# For podman-compose: bolt://neo4j:7687
# For standalone container with --network host: bolt://localhost:7687
NEO4J_URI=bolt://localhost:7687
NEO4J_DATABASE=neo4j
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=changeme123

# ==== AI Providers ====
EMBED_PROVIDER=ollama
CHAT_PROVIDER=ollama

# ==== Provider Endpoints ====
# For podman-compose: http://ollama:11434
# For standalone container with --network host: http://localhost:11434
OLLAMA_URL=http://localhost:11434
VLLM_URL=http://localhost:8001

# ==== Provider Models ====
OLLAMA_EMBED_MODEL=bge-m3
OLLAMA_EMBED_DIM=1024
OLLAMA_CHAT_MODEL=mistral:7b

# ==== API ====
API_PORT=8000

# ==== Retrieval ====
K_PROSE=40
K_LATEX=40
TOPK_FINAL=8

# ==== LaTeX handling ====
LATEX_ENABLE=true
LATEX_QUERY_WEIGHT=0.3
PROSE_QUERY_WEIGHT=0.6
BM25_LATEX_WEIGHT=0.1

# ==== Logging ====
LOG_LEVEL=INFO
LOG_DIR=/app/logs
